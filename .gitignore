/target

# Local snapshots created by the machine-health dashboard workflow.
/machine-health-*.duckdb

# Claude Code local settings + agent worktrees
.claude/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
